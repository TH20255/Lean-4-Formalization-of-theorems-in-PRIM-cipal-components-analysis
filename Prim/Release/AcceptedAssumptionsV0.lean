import Prim.Release.AcceptedAssumptionsV0.OffDiag
import Prim.Release.AcceptedAssumptionsV0.Upper

/-!
Compatibility aggregate for the accepted-assumptions v0 release spine.

Use `Prim.Release.AcceptedAssumptionsV0.Upper` for the lightest
upper-triangular publication-facing entrance, and
`Prim.Release.AcceptedAssumptionsV0.OffDiag` for the stronger full off-diagonal
sibling.  This aggregate preserves the historical all-in-one v0 release import.
-/
